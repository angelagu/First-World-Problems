<%= form_for(@first_world_problem) do |f| %>
  <% if @first_world_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@first_world_problem.errors.count, "error") %> prohibited this first_world_problem from being saved:</h2>

      <ul>
      <% @first_world_problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :problem %><br>
    <%= f.text_field :problem %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
